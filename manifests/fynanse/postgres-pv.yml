apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-volume
  namespace: fynanse
  labels:
    type: local
    app: postgres
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/data/postgresql"